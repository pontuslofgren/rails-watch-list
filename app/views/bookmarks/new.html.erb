<%= simple_form_for [@movie || @list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <% if @movie.present? %>
    <%= f.association :list %>
  <% else %>
    <%= f.association :movie %>
  <% end %>
  <%= f.button :submit, class:'btn btn-primary' %>
<% end %>
